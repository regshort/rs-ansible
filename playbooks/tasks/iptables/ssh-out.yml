---
- name: Allow outgoing SSH
  ansible.builtin.iptables:
    comment: "Allow outgoing SSH"
    chain: OUTPUT
    out_interface: "{{ interface }}"
    protocol: tcp
    destination_port: 22
    match:
      - state
      - tcp
    ctstate:
      - NEW
      - ESTABLISHED
    jump: "{{ jump }}"
