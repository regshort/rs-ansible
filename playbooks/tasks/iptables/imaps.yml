---
- name: Allow IMAPS
  ansible.builtin.iptables:
    comment: "Allow IMAPS"
    chain: INPUT
    in_interface: "{{ interface }}"
    protocol: tcp
    destination_port: 993
    match:
      - state
      - tcp
    ctstate:
      - NEW
      - ESTABLISHED
    jump: "{{ jump }}"

- name: Allow IMAPS
  ansible.builtin.iptables:
    comment: "Allow IMAPS"
    chain: OUTPUT
    out_interface: "{{ interface }}"
    protocol: tcp
    source_port: 993
    match:
      - state
      - tcp
    ctstate:
      - ESTABLISHED
    jump: "{{ jump }}"
