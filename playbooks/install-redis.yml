---
- name: Install redis
  hosts: data*
  vars_files:
    - "../vars/index.yml"
  remote_user: "{{ unix_user }}"
  become: true
  vars:
    redis_port: 6379
    redis_bind_interface: 0.0.0.0
  roles:
    - role: geerlingguy.redis
